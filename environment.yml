name: cvt2bids
channels:
  - conda-forge
  - defaults
dependencies:
  - ipython
  - setuptools
  - numpy
  - ipykernel
  - python=3
  - pandas
  - dcm2niix==1.0.20201102
  - pydicom
  - pip
  - pip:
      - nibabel
      - setuptools-scm
      - python-dotenv
